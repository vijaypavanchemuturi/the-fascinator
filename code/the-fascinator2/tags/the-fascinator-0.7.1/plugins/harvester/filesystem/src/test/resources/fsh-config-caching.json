{
    "harvester": {
        "type": "file-system",
        "file-system": {
            "targets": [ {
                "baseDir": "${test.dir}"
            } ],
            "cacheDir": "${test.cache.dir}"
        }
    }
}
