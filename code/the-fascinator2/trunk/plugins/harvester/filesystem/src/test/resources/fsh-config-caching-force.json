{
    "harvester": {
        "type": "file-system",
        "file-system": {
            "targets": [ {
                "baseDir": "${test.dir}",
                "force": true
            } ],
            "cacheDir": "${test.cache.dir}"
        }
    }
}
